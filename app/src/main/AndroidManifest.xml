<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.sairamedicalstore">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".SairaMedicalStoreApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo2"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.login.LoginActivity"
            android:theme="@style/AppTheme">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.AddNewMedicine" />
        <activity android:name=".ui.search.SearchActivity" />
        <activity android:name=".ui.ProductDetailsActivity" />
        <activity android:name=".ui.CreateOrUpdateOfferActivity" />
        <activity android:name=".ui.CreateOrUpdatePoster" />
        <activity android:name=".ui.PosterDetailsActivity" />
        <activity android:name=".ui.CartActivity" />
        <activity android:name=".ui.PrescriptionsActivity" />
        <activity android:name=".ui.AddNewPrescriptionActivity" />
        <activity android:name=".ui.address.DeliveryAddressActivity" />
        <activity android:name=".ui.address.AddNewAddressActivity"></activity>
    </application>

</manifest>